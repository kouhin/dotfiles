;;; init-sws-mode.el --- Init sws-mode

;;; Commentary:

;;; Code:

;; Required:
;; - coffee-script
;; - coffeelint
;; - jade
;; - stylus

(depends 'coffee-mode)
(depends 'sws-mode)
(depends 'jade-mode)
(depends 'stylus-mode)
(depends 'pug-mode)

(provide 'init-sws-mode)
;;; init-sws-mode.el ends here
